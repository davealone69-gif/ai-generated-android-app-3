<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Use the base Theme.Material3.DayNight.NoActionBar. 
         Ensure the dependency implementation("androidx.compose.material3:material3") 
         is present in build.gradle.kts to provide these resources. -->
    <style name="Theme.DroidCraft" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Explicitly define color attributes to prevent resolution errors -->
        <item name="colorPrimary">@android:color/holo_blue_dark</item>
        <item name="colorSecondary">@android:color/holo_green_dark</item>
        <item name="android:windowBackground">?attr/colorSurface</item>
    </style>
</resources>